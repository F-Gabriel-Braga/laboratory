<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Dados</h1>
    <% if(exibir) { %>
        <h2>Nome</h2>
        <h3><%= nome %></h3>
        <h2>Linguagem</h2>
        <h3><%= linguagem %></h3>
    <% } else { %>
        <h2>Nenhum dado!</h2>
    <% } %>
    
    <h2>Produtos</h2>
    <ul>
        <% produtos.forEach(function(produto) { %>
            <li>
                <strong>Nome:</strong> <%= produto.nome %> | <strong>Preço:</strong> <%= produto.preco %>
                </li>
        <% }) %>
    </ul>

    <img src="/images/eyecatch_160417.gif">
</body>
</html>